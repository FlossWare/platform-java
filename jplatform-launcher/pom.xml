<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.flossware.jplatform</groupId>
        <artifactId>jplatform-parent</artifactId>
        <version>1.0</version>
    </parent>

    <artifactId>jplatform-launcher</artifactId>
    <name>JPlatform Launcher</name>
    <description>Platform bootstrap and launcher</description>

    <dependencies>
        <!-- Core JPlatform modules -->
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-messaging</artifactId>
        </dependency>

        <!-- New feature modules -->
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-config</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-fs-watcher</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-rest-api</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-web-console</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-metrics-jmx</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.flossware.jplatform</groupId>
            <artifactId>jplatform-metrics-prometheus</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Jackson for JSON/YAML -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>org.flossware.jplatform.launcher.PlatformLauncher</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
